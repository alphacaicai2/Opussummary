# OpusBrief - AI 智能简报系统

## 项目定位

这是一个**独立的简报生成系统**，与实时推送系统完全分离。

**核心流程**：Miniflux RSS → 筛选分组/时间 → LLM 分析/总结 → 多平台推送

---

## 用户背景

作为 Financial Advisor，主要工作：
1. 帮助 AI Startup 从 VC 融资（关注中美 AI 投融资市场）
2. 帮助美国 VC（尤其是华人 VC）处理被投公司股权出售（二级市场）

---

## 功能需求

### 1. 信息源配置
- 连接 Miniflux RSS 后端（已部署在 VPS）
- 选择哪些分组作为简报信息源
- 选择时间范围（最近 N 小时的文章）

### 2. LLM 配置
- 支持多个主流 LLM 提供商（预设 Base URL）
- 用户只需输入 API Key
- 支持自定义 Base URL
- 注意：智谱等公司的国内/国外站点 Base URL 不同

**预设提供商**：
| 提供商 | Base URL |
|--------|----------|
| OpenAI | `https://api.openai.com/v1` |
| Anthropic | `https://api.anthropic.com/v1` |
| SiliconFlow | `https://api.siliconflow.cn/v1` |
| 智谱国内 | `https://open.bigmodel.cn/api/paas/v4` |
| 智谱国际 | `https://open.bigmodel.us/api/paas/v4` |
| Groq | `https://api.groq.com/openai/v1` |
| DeepSeek | `https://api.deepseek.com/v1` |
| 自定义 | 用户填写 |

### 3. 简报模板

| 模板 | 核心价值 | 筛选/生成逻辑 |
|------|----------|---------------|
| **通用格式** | FA 日常信息流 | 基于用户工作背景，智能筛选融资动态 + AI 产品 + 二级市场机会 |
| **投融资格式** | 融资情报 | 提取融资轮次/金额/投资方/赛道，表格化呈现 |
| **AI 产品格式** | 产品情报 | 模型发布、新产品、赛道探索、大厂动向（产品层面，非论文） |
| **公众号格式** | 流量选题 | 焦虑点/共鸣点/争议点/爆款潜力，给创作灵感 |

### 4. 调度配置
- 定时触发（具体时间点）
- 时区选择：北京 / 纽约 / 洛杉矶 / 伦敦
- 支持手动触发

### 5. 推送平台
- **优先**：Discord Webhook
- **次要**：企业微信群机器人 Webhook（待定，可能不做）

### 6. 历史记录
- 简报存档
- 支持查询历史简报
- 保留原文链接

---

## 技术方案

### 启动命令
```bash
python main.py              # 启动 Web UI + 简报定时任务
python main.py web          # 仅启动 Web UI（配置模式）
python main.py generate     # 手动生成一次简报（交互式选择模板）
python main.py test-llm     # 测试 LLM 连接
python main.py test-miniflux # 测试 Miniflux 连接
```

### 项目结构
```
OpusSummary/
├── main.py              # 入口
├── config.json          # 配置文件
├── database.py          # SQLite 数据库
├── miniflux_client.py   # Miniflux API 客户端
│
├── briefing/            # 简报模块
│   ├── __init__.py
│   ├── generator.py     # 简报生成核心逻辑
│   ├── templates.py     # 四种模板的 Prompt
│   └── scheduler.py     # 定时任务调度
│
├── llm/                 # LLM 模块
│   ├── __init__.py
│   ├── providers.py     # 提供商预设配置
│   └── client.py        # 统一 LLM 客户端
│
├── sender/              # 推送模块
│   ├── __init__.py
│   ├── discord.py       # Discord Webhook
│   └── wechat.py        # 企业微信（待定）
│
├── web/                 # Web UI
│   ├── __init__.py
│   ├── server.py        # FastAPI 服务
│   └── static/          # 前端文件
│
├── data/                # 数据存储
│   └── briefings.db     # SQLite 数据库
│
├── requirements.txt
├── Dockerfile
└── docker-compose.yml
```

### 数据库设计

```sql
-- LLM 配置
CREATE TABLE llm_configs (
    id          INTEGER PRIMARY KEY,
    name        TEXT,
    provider    TEXT,           -- openai/anthropic/siliconflow/...
    base_url    TEXT,
    api_key     TEXT,           -- 加密存储
    model       TEXT,
    is_default  BOOLEAN DEFAULT 0,
    created_at  TEXT
);

-- Webhook 配置
CREATE TABLE webhooks (
    id          INTEGER PRIMARY KEY,
    name        TEXT,
    type        TEXT,           -- discord/wechat_work
    url         TEXT,
    is_default  BOOLEAN DEFAULT 0,
    created_at  TEXT
);

-- 简报任务配置
CREATE TABLE briefing_tasks (
    id          INTEGER PRIMARY KEY,
    name        TEXT,
    template    TEXT,           -- general/investment/ai_product/wechat_mp
    schedule    TEXT,           -- Cron 表达式 或 "manual"
    timezone    TEXT,           -- Asia/Shanghai 等
    categories  TEXT,           -- JSON: 选中的分组ID列表
    time_range  INTEGER,        -- 时间范围(小时)
    llm_config_id INTEGER,      -- 关联使用的 LLM 配置
    webhook_ids TEXT,           -- JSON: 推送到的 webhook ID 列表
    enabled     BOOLEAN DEFAULT 1,
    created_at  TEXT
);

-- 简报历史
CREATE TABLE briefings (
    id          INTEGER PRIMARY KEY,
    task_id     INTEGER,        -- 关联 briefing_tasks
    template    TEXT,
    content     TEXT,           -- 生成的简报内容 (Markdown)
    content_html TEXT,          -- HTML 格式（可选）
    article_count INTEGER,      -- 包含文章数
    source_start TEXT,          -- 素材时间范围起点
    source_end   TEXT,          -- 素材时间范围终点
    sent_to     TEXT,           -- JSON: 推送到的 webhook 列表
    created_at  TEXT
);

-- 素材缓存（从 Miniflux 拉取的文章）
CREATE TABLE articles (
    id          INTEGER PRIMARY KEY,
    entry_id    INTEGER UNIQUE, -- Miniflux entry ID
    category_id INTEGER,
    title       TEXT,
    url         TEXT,
    published_at TEXT,
    content     TEXT,
    fetched_at  TEXT
);

-- 用户偏好（通用模板用）
CREATE TABLE user_preferences (
    key         TEXT PRIMARY KEY,
    value       TEXT,
    updated_at  TEXT
);
```

---

## Web UI 功能模块

### 1. 配置页面
- Miniflux 连接配置（URL + Token）
- LLM 提供商配置（下拉选择 + API Key 输入）
- Webhook 配置（Discord URL 输入 + 测试按钮）

### 2. 简报任务管理
- 创建/编辑/删除简报任务
- 选择模板类型
- 选择 Miniflux 分组（多选）
- 设置时间范围
- 设置定时（时间 + 时区）
- 选择推送目标

### 3. 手动生成
- 选择模板
- 选择分组
- 选择时间范围
- 预览素材数量
- 一键生成并推送

### 4. 历史记录
- 按时间/模板筛选
- 查看简报内容
- 重新推送

---

## 状态

- [x] 需求梳理
- [x] 技术方案设计
- [ ] 数据库实现
- [ ] Miniflux 客户端调整
- [ ] LLM 模块实现
- [ ] 简报生成模块实现
- [ ] 推送模块实现
- [ ] Web UI 实现
- [ ] 定时任务实现
- [ ] 测试与部署
